## Epsilon
```py
class Solution:
    def maxFrequencyElements(self, nums: List[int]) -> int:
        d = defaultdict(int)

        for i in list(set(nums)):
            d[nums.count(i)] += 1
        
        max_key = max(d.keys())
        return max_key*d[max_key]
```
17 ms </br>
13.3 MB
